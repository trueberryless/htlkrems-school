git config --global user.email "h.macho@htlkrems.at"
git config --global user.name "Herwig Macho"

git clone https://bitbucket.org/username/irgendein_repository

cd c:\.......\irgendein_repository: 

änderungen durchführen

git add .
git commit -m "juchee, was für eine schöne Änderung"
git push

Visual Studio Projekt erzeugen

git add .
git commit -m "Hello, World"
git push


